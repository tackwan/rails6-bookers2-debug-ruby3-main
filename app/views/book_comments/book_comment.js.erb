$(".comments_area").html("<%= escape_javascript(render partial: 'book_comments/book_comment', locals: {book: @book)} %>");
$(".comments_area").html("<%= j(render 'book_comments/book_comment', book: @book) %>");

$(".comment").val('');

